import { useTheme } from "@/app/hooks/useTheme"

const Footer = () => {
    const theme = useTheme();

    return (
        <div className="h-28">
            
        </div>
    )
}

export default Footer;